{
    "name": "calc-invaders",
    "type": "module",
    "version": "1.0.0",
    "scripts": {
    "prebuild": "node --trace-uncaught --unhandled-rejections=strict ./scripts/buildStages.js",
    "build": "vite build",
    "dev": "vite",
    "build:stages": "node ./scripts/buildStages.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
    "devDependencies": {
        "jsdom": "^27.0.0",
        "vite": "^5.0.0",
        "vitest": "^2.0.0"
    }
}
